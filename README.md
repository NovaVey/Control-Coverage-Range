# Control-Coverage-Range
An attack range for the whole agent-security stack — runs scenarios through the real assembled layers and reports which control caught each technique, which missed, and which cells nothing covers.
